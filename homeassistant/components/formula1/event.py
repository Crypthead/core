# TODO Look at the calendar events and our user config
# If calendar event in the timerange specified by user
#      send a event of upcoming race, then add to something so we don't send the event again
# If new standings != old, also send an event