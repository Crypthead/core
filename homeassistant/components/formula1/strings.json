{
  "config": {
    "step": {
      "user": {
        "title": "Configure the integration",
        "description": "Select what you are interested in",
        "data": {
          "show_calendar": "Add an entity which shows the upcoming races in Home Assistant",
          "only_show_race_event": "In the calendar, only show the final race vents or also include the practices, prints and qualifications",
          "show_driver_standings": "Add an entity which shows the current driver standings in Formula 1",
          "show_team_standings": "Add an entity which shows the current team standings in Formula 1"
        }
      }
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "unknown": "[%key:common::config_flow::error::unknown%]"
    },
    "abort": {
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    }
  }
}
